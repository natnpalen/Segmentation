function output_filename = save_density_results(outputDir, gradient_map, labels_kmeans, mask, ds, k, hu_min, hu_max, sigma_mm)
% SAVE_DENSITY_RESULTS  Persist analysis results next to pipeline outputs.

output_filename = fullfile(outputDir, 'density_analysis_results.mat');
save(output_filename, ...
    'gradient_map', ...
    'zone_map_kmeans_smoothed', ...
    'scaphoid_mask', ...
    'ds', ...
    'k', ...
    'hu_min', 'hu_max', 'sigma_mm');

% Where zone_map_kmeans_smoothed & scaphoid_mask variables come from:
% Expect caller to `assignin` locals or build the struct before calling if desired.
end
